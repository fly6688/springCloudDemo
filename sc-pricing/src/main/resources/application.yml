server:
  port: 8002
spring:
  application:
    name: sc-pricing-provider

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8811/eureka/
    lease:
      duration: 5
